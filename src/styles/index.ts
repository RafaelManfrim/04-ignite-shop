import { createStitches } from '@stitches/react'

export const { 
  config,
  createTheme,
  globalCss,
  theme,
  keyframes,
  css,
  styled,
  getCssText,
} = createStitches({
  theme: {
    colors: {
      primary: '#8257e6',
    },
  }
})

